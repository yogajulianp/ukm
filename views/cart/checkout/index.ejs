<%- include('../../templates/header')-%>
<section id="cart_items">
  <div class="container">
    <div class="table-responsive cart_info">
      <table class="table table-condensed">
        <!-- <thead>
          <tr class="cart_menu">
            <td class="image">Item</td>
            <td class="description"></td>
            <td class="price">Price</td>
            <td class="quantity">Quantity</td>
            <td class="total">Total</td>
            <td></td>
          </tr>
        </thead> -->
        <tbody>
     
          <% for(var i = 0; i < data.length; i++) { %> <%-
          include('checkout_item', {data:data[i]})-%> <% } %>
    
            <td colspan="3">&nbsp;</td>
            <td colspan="2" >
              <table class="table table-condensed total-result">
                <tbody>
                  <tr>
                    <td>Cart Sub Total</td>
                    <td><%=TotalPrice%></td>
                  </tr>
                 
                  <tr class="shipping-cost">
                    <td>Shipping Cost</td>
                    <td>Free</td>
                  </tr>
                  <tr>
                    <td>Total</td>
                    <td><span><%= TotalPrice%></span></td>
                  </tr>
                  <tr>
                    <td>
                      <a class="btn btn-default update" href="/cart">Cancel</a>
                      <form action="/checkout/pay" method="post">
                        <input type="text" value="<%= TotalPriceRaw%>" name="payment" id="payment" hidden>
                        <input type="submit"  class="btn btn-default check_out" value="Pay Now" >
                    </form>
                    </td>
                  </tr>
                </tbody>
              </table>
            </td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
</section>

<%- include('../..//templates/footer')-%>
